public class duplicateelementinarr {

    // duplicate element in arr with O(n) complexicity and without hash
    //make traverse an array and % every elemnt with no. of elemnts and add n add the modulo answer in an array
    // and divide it by n if frequency is >2 return element
}
